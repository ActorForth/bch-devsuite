{% if config.slp %}
export MONGO_USER={{ config.slp.username }}
export MONGO_PASS={{ config.slp.password }}
{% endif %}
{% if config.local_node %}
export RPC_USERNAME={{ config.local_node.rpc_conf.username }}
export RPC_PASSWORD={{ config.local_node.rpc_conf.password }}
{% endif %}
