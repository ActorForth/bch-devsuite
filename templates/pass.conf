{% if state.slp %}
export MONGO_USER={{ state.slp.username }}
export MONGO_PASS={{ state.slp.password }}
{% endif %}

{% if state.local_node %}
export RPC_USERNAME={{ state.local_node.rpc_conf.username }}
export RPC_PASSWORD={{ state.local_node.rpc_conf.password }}
{% endif %}
